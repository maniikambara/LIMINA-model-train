# Gabungan dependensi kedua pipeline.
# - sectors_fetcher/ + preprocessing/notebook/ (produksi, feature-random-forest)
# - limina/ + notebooks/ (arsip, LIMINA/AMBANG)
# Untuk paket yang dipakai keduanya, versi minimum yang lebih tinggi dipakai.

numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
joblib>=1.4.0
supabase>=2.31.0
python-dotenv>=1.0.0
nbclient>=0.10.0
nbformat>=5.10.0

# Hanya dipakai limina/ + notebooks/ (arsip)
tabulate>=0.9
pytest>=8.0
jupyter>=1.0

# Hanya dipakai api/ (FastAPI, lihat docs/PANDUAN-FASTAPI.md)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Hanya dipakai sectors_fetcher/ + preprocessing/notebook/ (produksi)
requests>=2.32.0
httpx>=0.28.0
pyarrow>=15.0.0
imbalanced-learn>=0.12.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0
ipykernel>=6.29.0
